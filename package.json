{
  "name": "simple-project-initializer",
  "version": "2.5.3",
  "description": "",
  "main": "main.js",
  "bin": {
    "simple-project-initializer": "main.js"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && npm version patch && tsc",
    "deploy": "npm run build && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "colorize-node": "^0.5.2",
    "nodemon": "^3.1.0",
    "rimraf": "^5.0.7",
    "typescript": "^5.4.5"
  }
}
