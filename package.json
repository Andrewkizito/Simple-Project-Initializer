{
  "name": "simple-project-initializer",
  "version": "2.5.4",
  "description": "",
  "main": "main.js",
  "bin": {
    "simple-project-initializer": "src/main.js"
  },
  "scripts": {
    "clean": "rimraf dist",
    "deploy:patch": "npm run clean && npm publish patch && rm *.tgz",
    "deploy:minor": "npm run clean && npm publish minor && rm *.tgz",
    "deploy:major": "npm run clean && npm publish major && rm *.tgz"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "colorize-node": "^0.5.2",
    "nodemon": "^3.1.0",
    "rimraf": "^5.0.7"
  }
}
