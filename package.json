{
  "name": "simple-project-initializer",
  "version": "2.6.0",
  "description": "",
  "main": "main.js",
  "bin": {
    "simple-project-initializer": "src/main.js"
  },
  "scripts": {
    "clean": "rimraf dist",
    "deploy:patch": "npm version patch && npm run clean && npm publish && rm *.tgz",
    "deploy:minor": "npm version minor && npm run clean && npm publish && rm *.tgz",
    "deploy:major": "npm version major && npm run clean && npm publish && rm *.tgz"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "colorize-node": "^0.5.2",
    "nodemon": "^3.1.0",
    "rimraf": "^5.0.7"
  }
}
